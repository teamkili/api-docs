---
title: "Authentication"
description: "Extract structured data from any unstructured file."
---

## API key

Sign up for a free account here and get an API key.

## Authentication

All API endpoints are authenticated by providing a Bearer token in the header.

```json
{
  "Authorization": "Bearer [Your API token]"
}
```
